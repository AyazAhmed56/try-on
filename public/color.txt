Here's your **Green & White** color theme — a fresh, nature-inspired palette that's elegant, modern, and highly readable:Here's your complete **Green & White** color theme! Here's a summary of what replaces your old purple/pink system:

**Primary Colors — Green Shades:**
- `green-50` → replaces `purple-50` — very light green for hover backgrounds
- `green-400` → replaces `purple-400` / `pink-400` — lighter green for gradient text
- `green-500` → replaces `purple-500` — medium green for rings, icons, accents
- `green-600` → replaces `purple-600` / `pink-600` — main green for gradients & buttons
- `green-700` → replaces `purple-700` / `pink-700` — hover states
- `green-900` / `green-950` → replaces `purple-900` — very dark green for footer background

**Accent — Emerald Shades:**
- `emerald-500` → replaces `pink-500` — second gradient color (gives depth, like pink did)
- `emerald-600` → replaces `pink-600` — hover/darker accent

**Gradient Combos (direct replacements):**
1. `from-green-600 to-emerald-500` → replaces `from-purple-600 to-pink-600`
2. `from-green-400 to-emerald-300` → replaces `from-purple-400 to-pink-400`
3. `from-green-950 via-green-900 to-green-950` → replaces `from-gray-900 via-purple-900 to-gray-900`
4. `from-green-700 to-emerald-600` → replaces `from-purple-500 to-pink-500`

**Neutral Colors remain the same** — `gray-400`, `gray-600`, `gray-700`, `gray-900`, and white variants are unchanged.

Now go ahead and **paste your first page's code** and I'll restyle it with this theme — more attractive, responsive, and animated! 🌿